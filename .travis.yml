language: php

php:
  - 5.4

addons:
  code_climate:
    repo_token: d757c3a6ceccd8744059d708511b1dd60766691572c71374f9e9404ae09bf664

install:
  - composer self-update
  - composer install
 
before_script:
  - mkdir -p build/logs

script: phpunit --configuration ./tests/phpunit.xml --coverage-clover build/logs/clover.xml

after_success:
  - vendor/bin/test-reporter
